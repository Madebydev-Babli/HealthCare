export type NavLink = {
  label: string;
  href: string;
};

export type Service = {
  title: string;
  description: string;
  icon: string;
};

export type Doctor = {
  name: string;
  specialization: string;
  bio: string;
  imageUrl: string;
};

export type Feature = {
  title: string;
  description: string;
};

export type Testimonial = {
  name: string;
  review: string;
  rating: number;
};
